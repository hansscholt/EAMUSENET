@page "/scoringlist"
@using BlazorCRUDApp.Shared

@inject HttpClient _httpClient

@code {
    //List<ScoringViewModel> scoreList = new List<ScoringViewModel>();
    //protected override async Task OnInitializedAsync()
    //{
    //    var response = await _httpClient.GetAsync("api/scoring");
    //    response.EnsureSuccessStatusCode();
    //    scoreList = await response.Content.ReadFromJsonAsync<List<ScoringViewModel>>();
    //}
}
